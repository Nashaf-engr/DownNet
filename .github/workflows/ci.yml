# CI/CD Pipeline (Currently Disabled - Deploy via Railway Docker)
# This workflow was disabled because Railway handles the build and test via Docker
# To re-enable: Remove the "if: false" condition below

name: CI/CD Pipeline

# Disabled for Railway Docker deployment
on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  info:
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Info
        run: echo "CI/CD disabled - Deploy via Railway Docker"
